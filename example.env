
# Firebase Client SDK Config (for Next.js app)
# These are used by the Firebase client SDK in your Next.js application.
# Copy these from your Firebase project settings (Project Overview > Project settings > General > Your apps > Firebase SDK snippet > Config).
NEXT_PUBLIC_FIREBASE_API_KEY="YOUR_API_KEY"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="YOUR_AUTH_DOMAIN"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="YOUR_PROJECT_ID"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="YOUR_STORAGE_BUCKET"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="YOUR_MESSAGING_SENDER_ID"
NEXT_PUBLIC_FIREBASE_APP_ID="YOUR_APP_ID"
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="YOUR_MEASUREMENT_ID" # Optional, for Google Analytics

# Google AI API Key (for Genkit)
# This API key is used by Genkit to interact with Google AI models (e.g., Gemini).
# Obtain this key from Google AI Studio (https://aistudio.google.com/app/apikey).
GOOGLE_API_KEY="YOUR_GOOGLE_AI_API_KEY"

# Firebase Admin SDK Config (for server-side scripts like populate-firestore-questions.ts)
# This is the path to your Firebase service account key JSON file.
# This file allows server-side scripts to authenticate with Firebase services with admin privileges.
# Download this file from Firebase Console > Project Settings > Service accounts > Generate new private key.
# Store the file securely in your project (e.g., in the root or a config directory, ensure it's in .gitignore if sensitive)
# and provide the absolute or relative path here.
# Example for a file in the project root: GOOGLE_APPLICATION_CREDENTIALS="./your-service-account-file.json"
# Example for an absolute path: GOOGLE_APPLICATION_CREDENTIALS="/Users/yourname/path/to/your-service-account-file.json"
GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/serviceAccountKey.json"
